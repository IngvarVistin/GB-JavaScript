1. Для практикума из занятия 7 продумать, где можно применить замыкания.
<O_o>

2. Не выполняя кода, ответить, что выведет браузер и почему:
________________________________________
if (!("a" in window)) {
    var a = 1;
}
alert(a);
-----------------------------------------
если без "!", то вывод 1, т.к. переменная "а" задается в области видимости.
в текущем случае - undefined, т.к. выводить нечего
если переменную "а" присвоить до if, то будет выведено присвоенное значение  
________________________________________
var b = function a(x) {
    x && a(--x);
};
alert(a);
-----------------------------------------
переменная "а" не задана. Выдаст ошибку.
________________________________________
function a(x) {
    return x * 2;
}
var a;
alert(a);
-----------------------------------------
Выдаст тело функции, аргумент в функцию не передан
________________________________________
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
-----------------------------------------
вывод 10, т.к. а = 10 )))
________________________________________
function a() {
    alert(this);
}
a.call(null);
-----------------------------------------
<O_o>
________________________________________
